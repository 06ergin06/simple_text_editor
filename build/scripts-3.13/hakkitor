#!/usr/bin/python3
import sys

# Ensure installed source directory is on the path
sys.path.insert(0, '/usr/share/hakkitor/src')

# Importing runs the application (main.py executes on import)
import main
